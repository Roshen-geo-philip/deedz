// TODO Implement this library.
import 'package:flutter/material.dart';

class ExploreCampaignPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Explore Campaign', style: TextStyle(color: Colors.white)),
        backgroundColor: Colors.black,
      ),
      backgroundColor: Colors.black,
      body: Center(
        child: Text('Explore Campaign Page', style: TextStyle(color: Colors.white)),
      ),
    );
  }
}
