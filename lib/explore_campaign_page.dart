import 'package:flutter/material.dart';

class ExploreCampaignPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Explore Campaigns', style: TextStyle(color: Colors.white)),
    );
  }
}
